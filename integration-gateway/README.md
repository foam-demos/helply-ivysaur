# Integration Gateway

API gateway managing all third-party integrations: Stripe, Zendesk, Freshdesk, HubSpot, Slack, Gong, PostHog. Handles authentication, rate limiting, webhook verification, and API normalization.

**Stack:** TypeScript, Node 20, Express, Prisma

**Run locally:** `npm run dev` (port 3001)